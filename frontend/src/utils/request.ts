export const BASE_URL = import.meta.env.VITE_BACKEND_URL ?? "http://localhost:8080";

/*
* const stores a variable called VBURL which is usually a standard environment variable,
* configured for the frontend, if it does not exist it is used on the local host.
*/

